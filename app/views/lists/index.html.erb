<ul class="nav justify-content-end">
  <li class="nav-item">
    <%= link_to "My lists", lists_path, class: 'btn text-decoration: none' %>
  </li>
  <li class="nav-item">
      <%= link_to "New list", new_list_path, class: 'btn text-decoration: none' %>
  </li>
</ul>

      <h1>My list</h1>

</div><ul class="list-group mb-3" style = " display:flex;; flex-direction:row; justify-content:space-around">
        <% @lists.each do |list| %>

    <div class="card " style="width: 18rem">
      <img class="card-img-top" src="..." alt="Card image cap">
      <div class="card-body">
        <h5 class="card-title"> <%= link_to list.name, list_path(list), class:"text-decoration-none text-color-black" %></h5>
      </div>
    </div>
  <% end %>
